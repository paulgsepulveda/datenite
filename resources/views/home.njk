{% extends 'master' %}

{% block content %}
  <div class="container" id="date">
    <div class="row" v-show="selectDate">
      <div class="col-md-12">
        <div class="panel panel-success">
          <div class="panel-heading">
            <h3 class="panel-title">Welcome to DateNite!</h3>
          </div>
          <div class="panel-body">
            <ul>
              <li>Are you bored with your usual routine?</li>
              <li>Have you ever been told that you need to be more spontaneous?</li>
              <li><strong>Is it getting late on Friday and you need to pretend you already thought of something on your own?</strong></li>
            </ul>
            <p>
              DateNite is here to help.
            </p>
          </div>
        </div>
        
        <div class="panel panel-info">
          <div class="panel-heading">
            <h3 class="panel-title">Find Your Date Now</h3>
          </div>
          <div class="panel-body">
            <p>Set your location and select one of the options below and DateNite will find you a randomly selected suggestion in your area:</p>
            <p>
            <div class="form-group">
              <div class="row">
              <div class="col-md-3">
                <label class="control-label">Zip Code: {% raw %} {{ userZip }} {% endraw %}</label>
                <input class="form-control" size="5"  maxlength="5" type="text" v-model="userZip">
              </div>
              </div>
            </div>
            </p>
            <p>
              <a v-on:click="sendRequest('restaurants', '{{ csrfToken }}' )" class="btn btn-info">Dinner</a>
              <a v-on:click="sendRequest('active', '{{ csrfToken }}' )" class="btn btn-info">Active / Outdoors</a>
              <a v-on:click="sendRequest('arts', '{{ csrfToken }}' )" class="btn btn-info">Arts / Music</a>
              <a v-on:click="sendRequest('nightlife', '{{ csrfToken }}' )" class="btn btn-info">Nightlife</a>
              <a v-on:click="sendRequest('surprise', '{{ csrfToken }}' )" class="btn btn-info">Surprise Me</a>
            </p>
          </div>
        </div>
      </div>
    </div>

    <div class="row" v-show="spinner">
      <div class="col-md-12 text-center">
        <p>Reticulating Splines...</p>
        <i class="fa fa-refresh fa-spin fa-3x fa-fw"></i>
        <span class="sr-only">Loading...</span>
      </div>
    </div>

    <div class="row" v-show="results">
      <div class="col-md-12">
        <div class="panel panel-primary">
          <div class="panel-heading">
            <h3 class="panel-title">Enjoy Your Date</h3>
          </div>
          <div class="panel-body">
            <h4>Location: </h4> <a :href="site_link"> {% raw %} {{ name }} {% endraw %}</a>
            <hr>
            <h4>Category: </h4> {% raw %} {{ date_type }} {% endraw %}
            <hr>
            <h4>Address: </h4> {% raw %} {{ address }} {% endraw %}
            <hr>
            <h4>Source: </h4> 
              <a :href="site_link"> <img class="img-responsive" width="100" height="64" src="/yelp/Yelp_trademark_RGB.png"></a>
              <img class="img-responsive" :src="rating">Reviews: {% raw %} {{ reviews }} {% endraw %}
            <hr>
            <h4>Not really feeling it?</h4> <p>Click below to re-roll. Don't abuse it though - it kinda goes against the whole spirit of spontaneity and adventure, y'know?</p>
            <p>
              <a v-on:click="sendRequest('restaurants', '{{ csrfToken }}' )" class="btn btn-primary">Dinner</a>
              <a v-on:click="sendRequest('active', '{{ csrfToken }}' )" class="btn btn-primary">Active / Outdoors</a>
              <a v-on:click="sendRequest('arts', '{{ csrfToken }}' )" class="btn btn-primary">Arts / Music</a>
              <a v-on:click="sendRequest('nightlife', '{{ csrfToken }}' )" class="btn btn-primary">Nightlife</a>
              <a v-on:click="sendRequest('surprise', '{{ csrfToken }}' )" class="btn btn-primary">Surprise Me</a>
            </p>
          </div>
        </div>
      </div>
    </div>  

  </div>
{% endblock %}


{% block js %}
  <script src="/js/app.js"></script>
{% endblock %}